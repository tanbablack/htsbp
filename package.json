{
  "name": "htsbp",
  "version": "1.0.0",
  "description": "Has This Site Been Poisoned? — AI Agent Threat Intelligence",
  "type": "module",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30",
    "node-fetch": "^3",
    "cheerio": "^1.0",
    "zod": "^3"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "esbuild": "^0.20",
    "tsx": "^4"
  },
  "scripts": {
    "build": "npm run copy-data && node build.mjs",
    "copy-data": "mkdir -p dist/api && cp -r data dist/api/",
    "collect": "npx tsx src/collect.ts",
    "recheck": "npx tsx src/recheck.ts",
    "validate-pr": "npx tsx src/validate-pr.ts",
    "dev": "npx netlify dev"
  }
}
